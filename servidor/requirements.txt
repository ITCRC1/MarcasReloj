# La lista de verdad esta en la raiz del repositorio, porque los constructores
# automaticos la copian sola antes que el resto de los archivos.
#
# Este archivo queda para que siga funcionando:
#   pip install -r servidor/requirements.txt

-r ../requirements.txt
